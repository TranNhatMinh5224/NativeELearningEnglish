// Fonts - Custom font files
// Để dùng custom fonts trong React Native:
// 1. Đặt file .ttf hoặc .otf vào folder này
// 2. Load fonts trong App.tsx bằng expo-font
// 3. Sử dụng trong StyleSheet

// Ví dụ fonts:
// - Roboto-Regular.ttf
// - Roboto-Bold.ttf
// - Montserrat-Regular.ttf
// - OpenSans-Regular.ttf

// Cách load fonts trong App.tsx:
/*
import * as Font from 'expo-font';
import { useFonts } from 'expo-font';

const [fontsLoaded] = useFonts({
  'Roboto-Regular': require('./assets/fonts/Roboto-Regular.ttf'),
  'Roboto-Bold': require('./assets/fonts/Roboto-Bold.ttf'),
});

// Sau đó dùng trong style:
fontFamily: 'Roboto-Regular'
*/

// Hiện tại: Dùng fonts mặc định của hệ thống
